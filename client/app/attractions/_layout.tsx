import { Stack } from "expo-router";

export default function AttractionsLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}